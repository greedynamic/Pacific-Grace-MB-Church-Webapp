<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- stylesheet -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>All Blog</title> 
</head>
<body>
    <div class="container">
        <h1 class="mb-4">All Blogs</h1>
        <a href="/blog/new/" class="btn btn-success">New Blog</a>

        <% if(blogs.length) { 
            for(var i=0; i < blogs.length; i++) { %> 
                <div class="card mt-4">
                    <div class="card-body">
                        <h4 class="card-title"><%= blogs[i].title %></h4>
                        <div class="card-subtitle text-muted mb-2"><%= blogs[i].published_at.toLocaleDateString() %></div>
                        <div class="car-text mb-2"><%- blogs[i].summary %></div>
                        <a href="/blog/<%= blogs[i].title %>" class="btn btn-primary">Read More</a>
                        <form action="/blog/del/<%= blogs[i].title %>" method="post" class="d-inline">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            <% }
        } %>  
    </div>

</body>
</html>